# Connection layer notes

Planned responsibilities:

- persistent session lifecycle
- platform-specific prompt handling
- Cisco pager suppression (`terminal length 0` / NX-OS equivalent)
- channel read/write helpers
- reconnect policy
- auth strategy (key first, password fallback)
